import classNames from 'classnames/bind';

import styles from './CreateCompany.module.scss';

const cx = classNames.bind(styles);

const CreateCompany = () => {
    return <div className={cx('wrapper')}>CreateCompany</div>;
};

export default CreateCompany;
